---
layout: nil
permalink: /search.json
---
[
  {% for post in site.posts %}
    {
      "id": "{{ site.url }}{{ post.url }}",
      "title": "{{ post.title | uri_escape }}",
      "date": "{{ post.date | date_to_xmlschema }}",
      "content": "{{ post.content | uri_escape }}"
    }
    {% if forloop.index < forloop.length %}
    ,
    {% endif %}
  {% endfor %}
]
